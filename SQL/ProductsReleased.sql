CREATE TABLE productsReleased (
    ITEM_NUMBER nvarchar(15);
    PRODUCT_NAME nvarchar(50);
    SEARCH_NAME nvarchar(50);
    PRODUCT_IMAGE nvarchar(10);
    PRODUCT_SUBTYPE nvarchar(20);
    PRICE_ZONE nvarchar(5);
    PRODUCT_STATE nvarchar(15);
)

/* Don't freak out that these are on two different DBs, I just wanted
to see them side by side to see relationships - Jared 6/15 */